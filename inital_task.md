Create a new project named <PROJECT_NAME> based on ./project_starter.md.

The new project path should be:

./<PROJECT_NAME>

Use ./project_starter.md as the complete boilerplate implementation contract.

Project type:

AI coding-agent starter repository

Primary stack:

Node.js 20+, no runtime dependencies for bootstrap scripts

Primary goal:

Create an executable boilerplate project that downloads, audits, and syncs AI coding-agent configs across Claude Code, Codex, Gemini CLI, Cursor, OpenCode, Grok Build, and GitHub Copilot.

Required agent support:
- Claude Code
- Codex
- Gemini CLI
- Cursor
- OpenCode
- GitHub Copilot

You must create a working project, not just Markdown.

Run:

npm run bootstrap

If download fails because network is unavailable, complete all local files and record the blocker in status.md.